[project]
name = "gridtrace-domain"
version = "0.1.0"
description = "Shared GridTrace domain formulas (pure Python, no heavy deps)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gridtrace_domain"]
